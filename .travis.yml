matrix:
  include:
    - os: windows
    - os: osx
      osx_image: xcode11
    - os: linux

language: cpp
install: skip

before_install:
  - if [ "$TRAVIS_OS_NAME" = "windows" ]; choco install 7zip ; fi
  - if [ "$TRAVIS_OS_NAME" = "windows" ]; choco install python3 ; fi

script: python3 rebuild-libs.py all
#deploy:
#  skip_cleanup: true
